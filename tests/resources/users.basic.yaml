parameters:
    suffix: Foo

Tests\Models\User:
    admin{1..3}:
        # TODO: replace with setAttribute if instance of Eloquent Model with option to disable
#        __construct:
#            -
        id: '10<current()>'
        username: 'admin<current()>'
        name: '<{prefix}> <name("male")> <{suffix}>'
        email: '<email()>'
